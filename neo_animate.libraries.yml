animate:
  version: VERSION
  neo:
    scope: front
  css:
    component:
      src/css/neo-animate.css: {}
  js:
    src/js/neo-animate.ts: {}
  dependencies:
    - core/drupal
    - core/once
    - core/drupalSettings
    - neo/animate

# Admin playground / gallery at /admin/config/neo/animate. A plain (non-neo)
# library so it needs no build step and loads on the admin page as-is; it only
# borrows the keyframes from neo/animate.
demo:
  version: VERSION
  css:
    theme:
      css/neo-animate-demo.css: {}
  js:
    js/neo-animate-demo.js: {}
  dependencies:
    - core/drupal
    - core/once
    - core/drupalSettings
    - neo/animate
